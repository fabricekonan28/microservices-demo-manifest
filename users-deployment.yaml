apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: monapp
  labels: { app: users-service }
spec:
  replicas: 2
  selector: { matchLabels: { app: users-service } }
  template:
    metadata:
      labels: { app: users-service }
    spec:
      containers:
        - name: users-service
          image: 5aae1a49af21.ngrok-free.app/monapp:10-users_service
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          env:
            - name: DATABASE_URL
              value: "postgresql+psycopg2://postgres:postgres@postgres:5432/microservices_db"
          readinessProbe:
            httpGet: { path: /health, port: 8001 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8001 }
            initialDelaySeconds: 15
            periodSeconds: 20
