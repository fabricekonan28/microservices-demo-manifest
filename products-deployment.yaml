# Déploiement Products Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
  namespace: monapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-service
  template:
    metadata:
      labels:
        app: products-service
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z db 5432; do echo "Waiting for PostgreSQL..."; sleep 2; done']
      containers:
        - name: products-service
          image: d0eb58e49125.ngrok-free.app/deploy:15-products-service
          envFrom:
            - configMapRef:
                name: app-config
          ports:
            - containerPort: 8002
          readinessProbe:
            httpGet:
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 5