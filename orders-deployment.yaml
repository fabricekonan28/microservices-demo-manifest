# Déploiement Orders Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: monapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z db 5432; do echo "Waiting for PostgreSQL..."; sleep 2; done']
      containers:
        - name: orders-service
          image: d0eb58e49125.ngrok-free.app/deploy:15-orders-service
          envFrom:
            - configMapRef:
                name: app-config
          ports:
            - containerPort: 8003
          readinessProbe:
            httpGet:
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 5